# Preserve upstream snapshots and patch context, including parser line-ending fixtures.
vendor/** -text -whitespace
patches/*.patch -text -whitespace
# Supplemental notices are verified against exact upstream byte checksums.
licenses/upstream/** -text
